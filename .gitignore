*.swp
*.swo
*.luac
.DS_Store
.luarc.json
.luarc.local.json
.tests/
# Auto-created by the plugin if smoke-tested in this repo's own cwd.
.auto-agents/
.auto-agents-config/
# Per-project CLAUDE.md is auto-injected by auto-agents into the agent's
# cwd. When the plugin is developed in-place inside this repo, that
# CLAUDE.md lands here — it's environment-specific and not part of the
# release artifact.
CLAUDE.md

# Design notes — kept locally for reference, not part of the release artifact.
PLAN.md
PERFORMANCE.md
LAYERED-ARCHITECTURE.md
